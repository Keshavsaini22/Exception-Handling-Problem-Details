'use strict'

module.exports = class Enums {
    static RECORD_STATUS = {
        ACTIVE: 'active',
        INACTIVE: 'inactive',
    }

    static USER_ROLE = {
        ADMIN: 'admin',
        USER: 'user',
    }
}